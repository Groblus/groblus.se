<div class="gallery">
  <h2>Bildgalleri</h2>
  <p>Här är några bilder från våra aktiviteter och träffar.</p>

  <div class="gallery-grid">
    {% for image in images %}
      <div class="gallery-item">
        <img src="{{ image.url }}" alt="{{ image.alt }}" />
        <div class="caption">{{ image.alt }}</div>
      </div>
    {% endfor %}
  </div>
</div>
