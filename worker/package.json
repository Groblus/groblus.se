{
  "name": "groblus-spelhyllan-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "test": "node --test scripts/*.node-test.mjs && vitest run",
    "db:migrate": "wrangler d1 migrations apply groblus --local",
    "invite": "node scripts/admin.mjs invite"
  },
  "dependencies": {
    "jose": "^6.2.12"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.1.8",
    "@cloudflare/workers-types": "5.20260911.1",
    "typescript": "^5.8.0",
    "vitest": "4.1.11",
    "wrangler": "4.131.1"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
